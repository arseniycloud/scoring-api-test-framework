"""Pytest fixtures, grouped by what they serve.

    environment.py — run configuration and CLI options
    clients.py     — the scoring API client
    database.py    — the scoring database
    data.py        — test data with its cleanup

Registered as plugins in the root `conftest.py`.
"""
